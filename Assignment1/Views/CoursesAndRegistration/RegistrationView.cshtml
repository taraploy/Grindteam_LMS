
@{
    ViewBag.Title = "RegistrationsView";
    Layout = "~/Views/Layout_MainLogin.cshtml";
    @*List<SelectListItem> l1 = new List<SelectListItem>();
    l1.Add(new SelectListItem() { Text = "stuff", Value = "0"});
    l1.Add(new SelectListItem() { Text = "stuff1", Value = "1"});
    l1.Add(new SelectListItem() { Text = "stuff2", Value = "2"});*@
}

@using Assignment1.Models
<div class="nav-pageview">
    <nav class="navbar navbar-dark">
        <span class="navbar-brand mb-0 h1"><img src="~/Images/GDS_LOGO.png" id="gds_title" /></span>
    </nav>
</div>
<div class="dashboard">
    <div class="row">
        <aside class="col-2 left-nav float-left">
            <p>
                <ul class="dashboard-nav">
                    <li><h5 class="nav_title">@Html.ActionLink("Home", "ReturnToView", "UserAccount")</h5></li>

                    <li><a href="#"><h5 class="nav_title">Registration</h5></a></li>

                    <li><h5 class="nav_title">@Html.ActionLink("Calendar", "Index", "Calendar")</h5></li>

                    <li><h5 class="nav_title">@Html.ActionLink("Profile", "Index", "Profile")</h5></li>

                    <li><a href="#"><h5 class="nav_title">Dummy 1</h5></a></li>

                    <li><a href="#"><h5 class="nav_title">Dummy 2</h5></a></li>

                    <li><a href="#"><h5 class="nav_title">Dummy 3</h5></a></li>
                </ul>
            </p>
        </aside>
        <div class="vl"></div>
        <div class="col-7">
            <div class="row justify-content-center">
                <form method="post">
                    <div>
                        @* Dropdown of all courses in database *@
                        @*@Html.DropDownListFor(a => a.course_name, Model.Cours, NULL, new { @class = "Cours" })*@

                       
                        @Html.DropDownList("CourseList", ViewBag.courselistname as SelectList, "Please Select a Course")

                        @* Register Button *@
                        <input type="submit" name="register" value="Register" formaction="@Url.Action("Register")" formmethod="post" />
                        @* Unregister Button *@
                        <input type="submit" name="unregister" value="Unregister" formaction="@Url.Action("Unregister")" formmethod="post" />
                    </div>
                </form>

                
            </div>
        </div>
    </div>
</div>



