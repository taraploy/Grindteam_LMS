@model IEnumerable<Assignment1.Department>
@{
    ViewBag.Title = "InstructorCview";
    Layout = "~/Views/Layout_MainLogin.cshtml";
}

@using Assignment1.Models

<div class="nav-pageview">
    <nav class="navbar navbar-dark">
        <span class="navbar-brand mb-0 h1"><img src="~/Images/GDS_LOGO.png" id="gds_title" /></span>
    </nav>
</div>
<div class="dashboard col-md-12">
    <div class="row">
        <aside class="col-md-2 left-nav float-left">
            <p>
                <ul class="dashboard-nav">
                    <li><h5 class="nav_title">@Html.ActionLink("Home", "ReturnToView", "UserAccount")</h5></li>

                    <li><h5 class="nav_title">@Html.ActionLink("Courses", "InstructorCview", "CoursesAndRegistration")</h5></li>

                    <li><h5 class="nav_title">@Html.ActionLink("Calendar", "Index", "Calendar")</h5></li>

                    <li><h5 class="nav_title">@Html.ActionLink("Profile", "Index", "Profile")</h5></li>

                    <li><h5 class="nav_title">@Html.ActionLink("Assignments", "CourseAssignmentList", "Assignment")</h5></li>

                    <li><a href="#"><h5 class="nav_title">Dummy 2</h5></a></li>

                    <li><a href="#"><h5 class="nav_title">Dummy 3</h5></a></li>
                </ul>
            </p>
        </aside>
        <div class="vl"></div>
        <div class="col-md-8 course-view">
            <div class="justify-content-center">
                <div class="centerHeader">
                    <h1>Courses</h1>
                </div>                              
                <hr />
                @Html.ActionLink("Add Course", "AddCourse", "CoursesAndRegistration", new { @class = "addCourseBtn" })
            </div>
            <div class="row">
                @{
                    foreach (var item in CourseCardList.CourseList)
                    {
                        <a>
                            <div class="card instructor-course-list">
                                <div class="card-body">
                                    <h5 class="card-title">@item.CourseNum</h5>
                                    <h6 class="card-subtitle mb-2 text-muted">@item.CourseName</h6>
                                    <h6 class="card-subtitle mb-2 text-muted">@item.InstructorFirstName @item.InstructorLastName</h6>
                                    <h6 class="card-subtitle mb-2 text-muted">@item.Building : Room @item.RoomNumber</h6>
                                    <h6 class="card-subtitle mb-2 text-muted">@item.StartTime - @item.EndTime @item.Days</h6>
                                </div>
                                <div class="detailCardBtn">
                                    @Html.ActionLink("Details", "InstructorCourseDetail", new { id = @item.CourseId }, new { @class = "detailCourseBtn" })
                                </div>
                            </div>
                        </a>
                    }
                }
            </div>
        </div>
    </div>
</div>
@*@using (Html.BeginForm())
    {
        <p>
            Search by Department: @Html.TextBox("SearchDepartment")
            <input type="submit" value="0" name="searchDepartment" @Html.ActionLink("Search", "InstructorCview", "CoursesAndRegistration")/>
        </p>
    }
    <form class="form-inline">
        <input class="form-control" name="searchKeyword" value="none" type="search" placeholder="Search" aria-label="Search" />
        <button class="btn btn-primary" type="submit" @Html.ActionLink("Search", "InstructorCview", "CoursesAndRegistration")>Search</button>
    </form>*@
@*<label for="search-keyword">Search: </label>
    <input type="text" id="search-keyword" asp-for="SearchString" />
    <input type="submit" value="Filter" />
    <br />*@


<!--<table class="table">
    <thead>
        <tr>
            <th>Course Number</th>
            <th>Course Name</th>
            <th>Course Description</th>
            <th>Instructor First Name</th>
            <th>Instructor Last Name</th>
            <th>Number of Credits</th>
            <th>Department</th>
            <th>Meeting Days</th>
            <th>Start Time</th>
            <th>End Time</th>
            <th>Location</th>
            <th>Room Number</th>
            <th>Capacity</th>
            <th></th>   <!-- Details button column -->
<!--<th></th>-->   <!-- Edit button column -->
<!--<th></th>-->   <!-- Delete button column -->
<!--</tr>
</thead>
<tbody>
    {
        foreach (Cours c in (dynamic)Model)
        {
            <tr>
                <td>Html.DisplayFor(modelItem => c.course_num)</td>
                <td>Html.DisplayFor(modelItem => c.course_name)</td>
                <td>Html.DisplayFor(modelItem => c.course_desc)</td>
                <td>Html.Display(Name.first_name)</td>
                <td>Html.Display(Name.last_name)</td>
                <td>Html.DisplayFor(modelItem => c.num_credits)</td>
                <td>Department</td>
                <td>Html.DisplayFor(modelItem => c.days_of_week)</td>
                <td>Html.DisplayFor(modelItem => c.start_time)</td>
                <td>Html.DisplayFor(modelItem => c.end_time)</td>
                <td>Html.DisplayFor(modelItem => c.building)</td>
                <td>Html.DisplayFor(modelItem => c.room_no)</td>
                <td>Html.DisplayFor(modelItem => c.max_capacity)</td>
                <td>Html.ActionLink("Details","","")</td>
                <td>Html.ActionLink("Edit","","")</td>
                <td>Html.ActionLink("Delete","","")</td>
            </tr>
        }


        }-->
@*@{
        int i = 0;
        foreach (var item in Model.Course.ToList())
        {
            <tr>
                <td>@Html.EditorFor(o => o.Course[i].course_num, new { @id = "CourseNum_" + i })</td>
                <td>@Html.EditorFor(o => o.Course[i].course_name, new { @id = "CourseName_" + i })</td>
            </tr>
        }
    }*@

@*<tr>
        <td>Course.courseName</td>
    </tr>
    foreach (var item in Model)
    {
        <tr>
            <td value="
    item.courseNum"></td>
            <td>item.courseName</td>
            <td>item.courseDesc</td>*@
@*<td>Html.DisplayFor(item => item.instructorFullName)</td>
    <td>Html.DisplayFor(item => item.numCredits)</td>
    <td>Html.DisplayFor(item => item.department)</td>
    <td>Html.DisplayFor(item => item.meetingDays)</td>
    <td>Html.DisplayFor(item => item.startTime)</td>
    <td>Html.DisplayFor(item => item.endTime)</td>
    <td>Html.DisplayFor(item => item.location)</td>
    <td>Html.DisplayFor(item => item.roomNum)</td>
    <td>Html.Displayfor(item => item.maxCapacity)</td>*@
<!--</tr>
}-->
<!--</tbody>
</table>-->
